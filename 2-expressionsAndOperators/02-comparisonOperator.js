// Equality 

    //equal operator
    '3' == 3; // -> true

    // strict equality operator
    '3' === 3; // -> false

    // Opposite operator
    '3' != 3; //-> false
    '3' !== 3; // ->true

    // greater than
    3 > 4; //-> false

    // less than
    3 < 4; //-> true (alligator rule)

    // greater than or equal to
    3 >= 4; // -> false

    //less than or equal to
    3 <= 4; // -> true

    // AND operator = &&
     // either both sides are true, or one/both side is false making both false
     let x = 13;
     x >= 5 && x <= 33;

    // OR operator = ||
    x > 3 || x == -3

    <= 8, !5
    let y = 1
    y <= 8 && y !== 5; 


